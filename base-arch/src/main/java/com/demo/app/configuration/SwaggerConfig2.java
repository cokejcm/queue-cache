package com.demo.app.configuration;

//@Configuration
//@EnableSwagger2
public class SwaggerConfig2 {
	/*
	 * @Bean public Docket productApi() { return new Docket(DocumentationType.SWAGGER_2) .select() .apis(RequestHandlerSelectors.basePackage("com.demo.app.controller")) .paths(PathSelectors.any()) .build() // .pathMapping("/rest") .apiInfo(apiInfo());
	 * 
	 * }
	 * 
	 * @SuppressWarnings("rawtypes") private ApiInfo apiInfo() { Contact contact = new Contact("Coke", "", "cokejcm@gmail.com"); return new ApiInfo("App REST API", "REST Services for APP", "1.0", "", contact, "", "", new ArrayList<VendorExtension>()); }
	 */
}
